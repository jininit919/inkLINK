flask
flask-limiter
werkzeug
resend
stripe==15.6.1
boto3
gunicorn
psycopg2-binary
pywebpush>=2.0.0
httpx[http2]>=0.27
reportlab>=4.0.0
Pillow>=10.0.0
sentry-sdk[flask]>=2.0.0
holidays>=0.60
tzdata>=2024.1

# HTTP klient pro Instagram Graph API. Explicitně, ne tranzitivně přes
# stripe/resend/boto3 — jejich bump by nám verzi vytáhl zpod nohou.
requests>=2.32
# Šifrování zdravotních údajů v souhlasech. Dosud se tahala jen tranzitivně
# přes pywebpush — explicitně proto, aby ji jejich update nemohl odnést.
cryptography>=42.0
# QR kód do sdílecí kartičky pro Instagram. Čistě pythonové, bez závislostí
# a bez nativního buildu — na Railway se nemá o co zaseknout.
segno>=1.6
